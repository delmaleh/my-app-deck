import React from "react"

export default function Card({city,lastname,name}) {
    return (
        <>
        <div>{city}</div>
        <div>{lastname}</div>
        <div>{name}</div> 
        
        </>   
    )
}